{{#if ideas}}
<div class="alert alert-success font-weight-bold">
  This is the public book list. Feel free to add books and notes. Alternatively, make an account and store your books
  privately.
</div>
{{/if}}

{{#each ideas}}
<div class="card card-body mb-3">
  <img src="{{bookCoverUrl}}" style="max-width: 200px" class="img-thumbnail" alt="Book cover of {{title}} by {{author}}">
  <h4>{{title}} by {{author}}</h4>
  <p>{{notes}}</p>
  <p>{{date}}</p>
  <a href="/ideas/edit/{{id}}" class="btn btn-secondary mb-2">EDIT</a>
  <form action="/ideas/{{id}}?_method=DELETE" method="POST">
    <input type="submit" value="DELETE" class="btn btn-danger btn-block">
  </form>
</div>
{{else}}
<div class="alert alert-danger font-weight-bold">
  No books on the public list. Feel free to add some. Alternatively, make an account and store your books privately.
</div>

{{/each}}